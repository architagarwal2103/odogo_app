enum UserRole { commuter, driver }
enum DriverMode { online, offline, busy }
enum TripStatus { pending, confirmed, ongoing, scheduled, completed, cancelled }
enum NotificationType { tripUpdate, demandAlert, systemAlert }